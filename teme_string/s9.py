# Replace the first 2 "e" in import_numbers
import_numbers = "Today we had 280 clients. One more e"

# replace e cu a si de cate ori sa il inlocuiasca.
new = import_numbers.replace('e', 'a', 3)
print(new)
